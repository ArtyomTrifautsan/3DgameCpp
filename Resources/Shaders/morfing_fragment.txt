#version 460


in vec3 v_pos;

layout(binding = 0) uniform sampler2D InTexture;

uniform vec3 color;

out vec4 fragment_color;

void main() {
    fragment_color = vec4((v_pos.x + 1) / 2, (v_pos.y + 1) / 2, (v_pos.z + 1) / 2, 1.0);
    //fragment_color = vec4(color, 1.0);
};

